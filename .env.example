# Copy this file to .env before running the server
# Change these values before deploying anywhere real

PORT=4000

# Secret used to sign login tokens - change this to a long random string
JWT_SECRET=change_this_to_a_long_random_secret_string

# Default admin account created automatically the first time the server runs
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
